<section id="calendar" class="relative w-full max-w-screen-2xl mx-auto">
  
  <div class="calendar-hero">
    <div class="calendar-overlay"></div>
    
    <div class="calendly-container">
      <!-- Calendly inline widget begin -->
      <div 
        class="calendly-inline-widget" 
        data-url="https://calendly.com/d/cqwn-kxh-s2v?hide_gdpr_banner=1" 
        data-resize="true">
      </div>
      <script type="text/javascript" src="https://assets.calendly.com/assets/external/widget.js" async></script>
      <!-- Calendly inline widget end -->
    </div>
  </div>
</section>

<style>
  /* Import Inter font */
  @import url('https://fonts.googleapis.com/css2?family=Inter:wght@400;600;700&display=swap');

  /* Main hero section with background image */
  .calendar-hero {
    position: relative;
    width: 100%;
    min-height: 700px;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 0;
    background: url('../assets/Female_Friends_Meeting_In_Cafe_original_4647221.png') center center no-repeat;
    background-size: cover;
    isolation: isolate;
  }

  /* Dark overlay */
  .calendar-overlay {
    position: absolute;
    left: 0;
    right: 0;
    top: 0;
    bottom: 0;
    background: rgba(0, 0, 0, 0.15);
    z-index: 0;
  }

  /* Calendly container */
  .calendly-container {
    width: 90%;
    max-width: 600px;
    z-index: 1;
    position: relative;
    backdrop-filter: blur(10px);
    border-radius: 4px;
    overflow: hidden;
    display: flex;
    flex-direction: column;
  }

  /* Calendly inline widget */
  .calendly-inline-widget {
    width: 100%;
    min-height: 450px;
    background: transparent !important;
    border: none;
    position: relative;
    z-index: 5;
  }

  /* Responsive adjustments */
  @media (max-width: 768px) {
    .calendly-container {
      width: 100%;
    }
  }
</style>

